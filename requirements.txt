# 后端运行依赖。
# 说明：前端为 web/ 下的 React 应用；本文件仅列出 Python 后端依赖。
fastapi==0.115.6
uvicorn[standard]==0.34.0
# 抽帧插件（video.frame-extraction）解码与图像质量计算依赖。
# imageio-ffmpeg 自带静态 ffmpeg 二进制，无需系统预装 FFmpeg。
imageio-ffmpeg==0.6.0
numpy==2.5.1
pillow==12.3.0
# 姿态·人脸标注插件（image.pose-face-annotation）自动识别依赖。
# 使用随包自带的 Haar 级联（cv2.data.haarcascades），无需额外下载模型权重，可离线运行。
# 注意：5.0 起官方 wheel 不再内置 Haar 级联，故固定在 4.13.x。
opencv-python-headless==4.13.0.92
