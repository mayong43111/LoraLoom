{
  "name": "dataset-plugin-template",
  "version": "0.1.0",
  "private": true,
  "description": "Dataset Toolkit 插件开发模板（独立项目）",
  "type": "module",
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
